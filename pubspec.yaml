name: diary_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  geocoding: ^2.1.0
  geolocator: ^10.1.0
  http: ^1.1.0
  image_picker: ^1.0.4
  mockito: ^5.4.2
  permission_handler: ^11.0.1

dev_dependencies:
  build_runner: any
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
